import { OrganizationAuthorityModel } from './organization-authority.model';

describe('OrganizationAuthorityModel', () => {
  it('should create an instance', () => {
    expect(new OrganizationAuthorityModel()).toBeTruthy();
  });
});
