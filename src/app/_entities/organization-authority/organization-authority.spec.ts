import { OrganizationAuthority } from './organization-authority';

describe('OrganizationAuthority', () => {
  it('should create an instance', () => {
    expect(new OrganizationAuthority()).toBeTruthy();
  });
});
